package main

import "fmt"


func main() {
	// var card string = "Ace of Spades"
	// := rely on Go compiler to infer the variable's type
	// we only use := when we are defining a new variable
	card := "Ace of Spades"
	
	fmt.Println(card)
}